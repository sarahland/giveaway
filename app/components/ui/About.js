const About = () => (
   <div className="about">
        <h1>About Give Away</h1>
        <p>An app for items to give up that uses no longer need or have any use for.
          We will be using Google API, Firebase, html, css, react bootstrap,
          react UI, react-native, and node.
        </p>
   </div>
)
export default About
